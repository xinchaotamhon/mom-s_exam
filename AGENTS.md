# Hướng dẫn dự án

1. Đọc `START_HERE.md` theo đúng thứ tự trước khi sửa dự án.
2. Dùng kỹ năng `prepare-project-work` khi có sẵn.
3. Ưu tiên đồ thị codebase-memory cho khám phá và phân tích mã đã được lập chỉ mục.
4. Không sửa hoặc ghi đè tài liệu Word nguồn.
5. Không lưu khóa bí mật, cookie hay thông tin đăng nhập trong dự án.
6. Mỗi thay đổi tính năng phải giữ các cổng kiểm tra cũ và thêm kiểm tra tập trung phù hợp.
7. Nội dung đáp án phải có xuất xứ; điểm chưa thể xác minh phải được ghi rõ, không đoán như sự thật.
8. Ghi trạng thái biến động trong `40-State` và kết quả chạy trong `50-Evidence`.
